
server:
  port: 8080
#  session-timeout: 30
#  tomcat.max-threads: 0
  tomcat.uri-encoding: UTF-8

spring:
  datasource:
    url : jdbc:mysql://localhost:3306/sup_base
    username : root
    password : 19980128yct
    driverClassName : com.mysql.cj.jdbc.Driver

mybatis:
  mapperLocations: classpath:mapper/*.xml
